import styles from "../landing.module.css";

export const Header =()=> (
    <header className={styles.header}>
        <div className={styles.orange}>logo</div>
        <nav className={styles.nav}>nav</nav>
    </header>
)